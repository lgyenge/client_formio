import { FilterFormPipe } from './filter-form.pipe';

describe('FilterFormPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFormPipe();
    expect(pipe).toBeTruthy();
  });
});
